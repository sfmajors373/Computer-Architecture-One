const readline = require('readline');
// take in input file
// initialize registers
// read line by line
// execute linee

fs.open('./input.txt', 'r', (err, data) => {
  if (err) {
    console.log(err);
  }
}

function isCommented(
